type Props = {
  id: any
  date?: string
  image: string
  title: string
  body: string
  slug: string
  className?: string
  dir?: "left" | "right" | "top" | "bottom"
  bottomChild?: React.ReactNode
}

export default Props
